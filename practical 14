14. Write a PROLOG program to implement insert(I, N, L, R) that inserts an item I into
Nth position of list L to generate a list R.

% Base case: insert I at position 1 by placing it at the head of L
insert(I, 1, L, [I|L]).

% Recursive case: reduce position N by 1 and insert in the tail
insert(I, N, [H|T], [H|R]) :-
    N > 1,
    N1 is N - 1,
    insert(I, N1, T, R).

Sample
?- insert(a, 3, [b, c, d, e], R).
R = [b, c, a, d, e].
