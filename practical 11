11. Write a PROLOG program to implement sumlist(L, S) so that S is the sum of a given
list L.

% Base case: sum of an empty list is 0
sumlist([], 0).

% Recursive case: sum is head plus sum of the tail
sumlist([H|T], S) :-
    sumlist(T, S1),
    S is H + S1.
Sample
?- sumlist([1, 2, 3, 4, 5], S).
S = 15.
